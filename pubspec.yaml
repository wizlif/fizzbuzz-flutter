name: fizzbuzz
description: A FizzBuzz application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  dio: ^3.0.9
  dash: ^0.5.3
  rxdart: ^0.24.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.1.3
  dash_generator: ^0.5.3

flutter:
  uses-material-design: true